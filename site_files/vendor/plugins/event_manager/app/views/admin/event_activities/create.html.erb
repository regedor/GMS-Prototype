<% if File.exists?(Rails.root.join("app", "views", params[:controller], "_new_sidebar.html.erb")) %>
  <% content_for :sidebar do -%>
    <%= render :partial => 'new_sidebar' %>
  <% end -%>
<% elsif File.exists?(Rails.root.join("app", "views", params[:controller], "_sidebar.html.erb")) %>
  <% content_for :sidebar do -%>
    <%= render :partial => 'sidebar' %>
  <% end -%>
<% end -%>

<div class="content">
  <div class="inner">
    <h2><%= I18n::t('admin.activities.form.new') %></h2>
    <% semantic_form_for :record, @record,
                         :url => admin_event_event_activities_path,
                         :html => { :class => 'create', :id => 'new_activity' } do |form| -%>
      <%= render :partial => 'form', :locals => {:form => form} %>
      <% form.buttons do -%>
        <%= form.commit_button I18n::t('admin.activities.form.create_button'),
                               :button_html => { :class => 'button positive' } %>
        <%= form.button_link   I18n::t('admin.activities.form.cancel_button'), admin_event_event_activities_path,
                               :button_html => { :class => 'button negative' } %>
      <% end -%>
    <% end -%>
  </div>
</div>

