<div class="content">
  <div class="inner">
    <h2><%= t("admin.events.index.title") %><h2>
    <% content_for :sidebar, render(:partial => 'index_sidebar') -%>
    <% @events.each do |event| %>
    <h4><%= link_to event.title, admin_event_path(event) %></h4>
    <% end %>
  </div>
</div>
