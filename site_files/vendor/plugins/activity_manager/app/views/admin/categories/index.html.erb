<div class="content">
  
  <% if @categories.empty? %>
    <p><%= I18n::t('admin.categories.list.empty') %></p>
  <% else %>

  <div id="categories">
    <% @categories.each do |category| -%>
    <div class="category">
     	<%= render :partial => 'category', :locals => {:project => @project, :category => category} %>
    </div>
        </td>
      </tr>
    <% end -%>
    </table>
  </div>  
  <% end %>
</div>
