<div class="content">
  <div class="inner">
    <h2><%= I18n::t('admin.user_optional_group_picks.form.edit') + ' - ' + link_to(h(@record.name), admin_user_optional_group_pick_path(@record)) %></h2>
    <% semantic_form_for :record, @record, 
                         :url => admin_user_optional_group_pick_path(@record), 
                         :html => { :method => :put, :id => "edit_user_optional_group_pick_#{@record.id}" } do |form| -%>
      <%= render :partial => 'form', :locals => { :form => form } %>
      <% form.buttons do -%>
        <%= form.commit_button I18n::t('admin.user_optional_group_picks.form.update_button'),
                               :button_html => { :class => 'button positive' } %>
        <%= form.button_link   I18n::t('admin.user_optional_group_picks.form.cancel_button'), admin_user_optional_group_picks_path,
                               :button_html => { :class => 'button negative' } %>
      <% end -%>
    <% end -%>
  </div>
</div>

