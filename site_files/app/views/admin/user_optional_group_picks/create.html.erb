<div class="content">
  <div class="inner">
    <h2><%= I18n::t('admin.user_optional_group_picks.form.new') %></h2>
    <% semantic_form_for :record, @record,
                         :url => admin_user_optional_group_picks_path,
                         :html => { :class => 'create', :id => 'new_user_optional_group_pick' } do |form| -%>
      <%= render :partial => 'form', :locals => { :form => form } %>
      <% form.buttons do -%>
        <%= form.commit_button I18n::t('admin.user_optional_group_picks.form.create_button'),
                               :button_html => { :class => 'button positive' } %>
        <%= form.button_link   I18n::t('admin.user_optional_group_picks.form.cancel_button'), admin_user_optional_group_picks_path,
                               :button_html => { :class => 'button negative' } %>
      <% end -%>
    <% end -%>
  </div>
</div>
