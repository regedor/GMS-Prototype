<div class="content">
  <div class="inner">
    <h2><%= I18n::t('admin.groups.form.edit') + ' - ' + link_to(h(@record.name), admin_group_path(@record)) %></h2>
    <% semantic_form_for :record, @record, 
                         :url => admin_group_path(@record), 
                         :html => { :method => :put, :id => "edit_group_#{@record.id}" } do |form| -%>
      <%= render :partial => 'form', :locals => { :form => form } %>
      <% form.buttons do -%>
        <%= form.commit_button I18n::t('admin.groups.form.update_button'),
                               :button_html => { :class => 'button positive' } %>
        <%= form.button_link   I18n::t('admin.groups.form.cancel_button'), admin_groups_path,
                               :button_html => { :class => 'button negative' } %>
      <% end -%>
    <% end -%>
  </div>
</div>
